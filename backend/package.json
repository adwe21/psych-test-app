{
  "name": "psych-test-backend",
  "version": "1.0.0",
  "description": "Backend for psychiatric test web application",
  "main": "app/main.py",
  "scripts": {
    "start": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.95.0",
    "uvicorn": "^0.21.1",
    "pydantic": "^1.10.7"
  },
  "devDependencies": {
    "pytest": "^7.2.2"
  }
}